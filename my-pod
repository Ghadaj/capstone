apiVersion: v1
kind: Pod
metadata:
  name:my-pod
  labels:
    app: mydockerwebapp 
# This is the pod template  
spec:
  containers:
  - name: sklearn
    image: ghadaj/mydockerwebapp
    command: ['sh', '-c', 'echo "Creating a pod ..." && sleep 3600']
    ports:
    - containerPort: 80
